import SwiftUI

enum PetPalette {
    // Sky
    static let skyTop = Color(red: 0.40, green: 0.70, blue: 0.95)
    static let skyBottom = Color(red: 0.65, green: 0.85, blue: 1.0)

    // Grass
    static let grassLight = Color(red: 0.30, green: 0.72, blue: 0.25)
    static let grassDark = Color(red: 0.22, green: 0.58, blue: 0.20)

    // Character
    static let bodyMain = Color(red: 0.55, green: 0.85, blue: 0.55)
    static let bodyDark = Color(red: 0.40, green: 0.68, blue: 0.40)
    static let bodyLight = Color(red: 0.70, green: 0.95, blue: 0.70)
    static let eyeWhite = Color.white
    static let eyePupil = Color(red: 0.15, green: 0.15, blue: 0.20)
    static let mouth = Color(red: 0.15, green: 0.15, blue: 0.20)
    static let blush = Color(red: 0.95, green: 0.55, blue: 0.55)

    // Dialogue box
    static let boxFill = Color(red: 0.08, green: 0.08, blue: 0.22)
    static let borderOuter = Color(red: 0.90, green: 0.75, blue: 0.30)
    static let borderInner = Color(red: 0.70, green: 0.55, blue: 0.20)
    static let textColor = Color(red: 0.95, green: 0.93, blue: 0.85)
    static let nameplateBg = Color(red: 0.08, green: 0.08, blue: 0.22)
    static let dimText = Color(red: 0.75, green: 0.72, blue: 0.62)
}
